# 프로토타입 템플릿

## 이 프로젝트는

피그마 와이어프레임을 기반으로 실제로 동작하는 프로토타입을 만들어 플로우와 인터랙션을 직접 사용해보고 문제점을 찾는 용도다.
실제로 사용해볼 수 있는 수준의 디자인 퀄리티가 필요하다.

---

## 기술 스택

- **프레임워크**: React + Vite
- **언어**: JavaScript
- **스타일링**: Tailwind CSS + CSS 변수 (tokens.css)
- **폰트**: Pretendard (src/styles/fonts.css에서 CDN 로드)
- **배포**: Vercel

---

## 핵심 원칙

- 피그마 디자인과 픽셀 단위로 동일하게 구현한다. 임의로 단순화하거나 간략하게 만들지 않는다.
- 피그마의 레이아웃, 간격, 폰트 크기/굵기, 색상, 모서리, 그림자 등 모든 스타일 속성을 그대로 반영한다.
- 피그마에 명시된 텍스트, 수치, 아이콘, 구조는 하나도 빠짐없이 반영한다.
- 아이콘과 그래픽은 `public/icons/`에 있는 것을 우선 사용한다. 없으면 아래 [아이콘] 섹션의 규칙을 따른다.
- 외부 패키지는 최소화한다. 꼭 필요한 것만 설치한다.

---

## 와이어프레임 해석 기준

- 레이아웃, 구조, 간격, 정보 계층은 와이어프레임을 그대로 따른다. 임의로 변경하지 않는다.
- 와이어프레임에 색상/타이포가 지정되어 있으면 그대로 반영하고, 지정되어 있지 않거나 플레이스홀더 수준이면 tokens.css 기준으로 채운다.
- 와이어프레임에 없는 상태(로딩, 빈 상태, 에러, 인터랙션 피드백 등)는 작업 전 질문 리스트에 포함해 확인한다. 임의로 만들지 않는다.

---

## 아이콘

- 아이콘은 우선 `public/icons/`에서 찾아 사용한다.
- 없는 아이콘은 lucide-react에서 가장 가까운 것을 사용한다.
- lucide-react에도 적절한 게 없으면 작업을 멈추고 사용자에게 요청한다. 임의로 대체 아이콘을 만들지 않는다.
- lucide-react로 대체한 아이콘은 작업 완료 보고 시 목록으로 알려준다 (나중에 로컬 SVG로 교체할 수 있도록).

---

## 프로토타입 작업

- 요청(제작, 수정, 페이지 추가 등)을 받으면 먼저 요청 내용을 분석한다.
- 빠지거나 불명확하거나 오류가 있으면 질문 리스트를 한번에 출력한다. 아래 항목을 우선 점검한다:
  - 없는 아이콘/에셋
  - 정의되지 않은 상태(로딩/빈 화면/에러/성공)
  - 화면 간 이동 방식(모달/새 페이지/바텀시트 등)
  - 버튼·입력 등 인터랙션의 피드백 방식
- 모든 답변을 받은 후 작업을 시작한다. 문제없으면 바로 작업.

---

## 디자인

- 색상은 반드시 `src/styles/tokens.css`의 CSS 변수를 사용한다. (`var(--color-primary-600)`)
- tokens.css에 없는 값은 하드코딩한다.
- tokens.css에 새로운 토큰을 임의로 추가하지 않는다.
- 컴포넌트 수정 과정에서 tokens.css는 절대 건들지 않는다.
- 폰트는 Pretendard만 사용한다.
- 모바일 화면 좌우 패딩은 16px 고정.
- 모바일 기준 너비: 고정값 사용 금지. 100vw 기준으로 유동적으로 대응한다.
- 모바일 기준 높이: 고정값 사용 금지. 100dvh 기준으로 유동적으로 대응한다.
- 고정 높이 대신 min-height: 100dvh, 스크롤 가능한 레이아웃으로 구성한다.
- OS: iOS 기반
- Safe Area는 env(safe-area-inset-top), env(safe-area-inset-bottom) CSS 변수로 대응한다.
- 스크롤은 momentum scroll 적용 (-webkit-overflow-scrolling: touch)
- 탭바, 내비게이션바는 fixed로 처리하고 콘텐츠 영역은 남은 높이를 채우도록 구성한다.
- 터치 타겟은 최소 44px 이상으로 유지한다.
- 화면 전환은 iOS 네이티브 느낌으로 슬라이드 인/아웃 처리한다.
- 간격, 모서리, 그림자 등 세부 스타일도 tokens.css 값 기준으로 다듬는다.
- 타이포그래피 크기/굵기, 색상 대비, 여백은 와이어프레임을 따르되 tokens.css 기반으로 개선한다.
- 테스트 환경은 모바일 크롬(웹)이다. 아래 항목을 반드시 적용한다.
  - 텍스트 선택, 길게 누르기 등 웹 기본 동작 비활성화 (user-select: none, -webkit-tap-highlight-color: transparent)
  - 핀치 줌 비활성화 (뷰포트 메타태그에 user-scalable=no 적용)
  - 스크롤 오버스크롤 바운스 효과 비활성화 (overscroll-behavior: none)
  - 브라우저 주소창/툴바 높이 변동 대응 (100dvh 사용, 콘텐츠가 가려지지 않도록)
  - 스테이터스바(시간, 배터리 등)는 만들지 않는다. 모바일 웹 환경에서 테스트하므로 OS가 자체 제공한다.

---

## 컴포넌트

- `src/components/`에 있는 공용 컴포넌트는 재활용한다.
- `src/components/`에 새로운 컴포넌트를 임의로 추가하지 않는다.
- 컴포넌트는 외부에서 완성된 것을 가져다 넣는다. 여기서 직접 만들지 않는다.
- 없는 UI는 화면 코드 안에서 직접 만든다.
- 페이지 파일명은 영문 소문자 kebab-case로 통일한다. (예: `home-screen.jsx`, `invest-detail.jsx`)

---

## 피그마

- 피그마 URL을 주면 `get_design_context`로 레이아웃, 구조, 스타일 속성을 정밀하게 파악한다.
- 피그마의 모든 요소를 픽셀 단위로 동일하게 구현한다. 폰트 크기, 굵기, 행간, 색상, 간격, 모서리, 패딩 등을 정확히 맞춘다.
- 피그마에 명시된 텍스트, 수치, 구조는 하나도 빠짐없이 그대로 반영한다. 생략하거나 합치지 않는다.
- `get_design_context`가 반환한 코드는 참고만 한다. 그대로 쓰지 않는다.
- 피그마 스크린샷과 실제 구현 결과가 시각적으로 구분되지 않아야 한다.

---

## 작업 완료 전 확인

- 완성 후 피그마 디자인과 나란히 비교해 레이아웃/간격/색상/폰트 차이가 있는지 스스로 점검한다.
- 차이가 있으면 이유를 보고하거나 즉시 수정한다.
- lucide-react로 대체한 아이콘 목록을 보고에 포함한다.

---

## 서비스 컨텍스트

- 서비스 개요 문서는 `docs/` 폴더 안에 있다. 파일명은 프로젝트마다 다를 수 있다.
- 작업 시작 전 `docs/` 폴더를 확인해 서비스 개요로 보이는 md 파일이 있으면 반드시 읽고 도메인 용어와 구조를 파악한 후 작업한다.
- `docs/` 안에 서비스 개요 파일이 없으면, 있어야 할 것 같은 상황(도메인 용어를 모르는 화면 작업 등)에서 사용자에게 먼저 물어본다.

---

## 파일 구조

```
src/
├── components/   ← 완성된 공용 컴포넌트 (직접 만들지 않음)
├── pages/
│   ├── v1/       ← 버전별 프로토타입
│   └── v2/
└── styles/
    ├── fonts.css  ← Pretendard CDN
    ├── index.css  ← @import fonts.css, tokens.css
    └── tokens.css ← 색상/타이포 CSS 변수
```

---

## 홈 페이지

- `src/App.jsx`에 홈 페이지가 있다. `/` 경로로 접근하면 버전 리스트를 보여준다.
- 새 버전(v4, v5 등)을 만들면 반드시 `src/App.jsx`의 `versions` 배열에 추가한다.
- 각 버전 항목에는 `key`(경로), `label`(표시명), `desc`(설명)을 포함한다.
- 각 항목은 새 탭(`target="_blank"`)으로 열린다.

---

## 배포 및 공유

"배포해줘" 명령 시 아래 순서로 실행:

1. `git add .`
2. `git commit -m "update"`
3. `git push`
4. 완료 후 아래 링크 표시:
   - GitHub: 현재 프로젝트의 GitHub 레포 URL
   - 팀원 공유: (Vercel URL) ← 홈 링크만 표시

---

## 리소스 관리 (public/)

- 사용자가 `public/`에 파일을 추가하면, 작업 시 아래 규칙에 따라 자동으로 정리한다.
- **이미지 파일** (png, jpg, webp 등)은 `public/images/`에 넣는다.
- **아이콘 파일** (svg)은 `public/icons/`에 넣되, 용도별 하위 폴더로 분류한다.
  - `icons/appbar/` — 앱바 아이콘
  - `icons/tab/` — 탭바 아이콘
  - `icons/graphic/` — 그래픽/일러스트 아이콘
  - `icons/finance/` — 금융사 로고
  - `icons/` 루트 — 범용 아이콘
- **파일명 규칙**: 영문 소문자 kebab-case. 한글, 공백, camelCase 금지.
  - 예: `한약재 먹으며 건강히 키우는 상품.png` → `product-herbal.png`
  - 예: `Notification Icon.svg` → `notification.svg`
  - 예: `moneyBag.svg` → `money-bag.svg`
- `favicon.svg`, `logo.svg`는 루트에 유지한다.
- 파일 이동/이름 변경 시 소스 코드의 모든 참조 경로도 함께 업데이트한다.
- 미사용 파일은 발견 시 삭제한다.

---

## 참고

- `CLAUDE.md`는 Claude Code 전용 설정 파일이다. Codex는 이 파일을 읽지 않으며 참고하지 않는다.
